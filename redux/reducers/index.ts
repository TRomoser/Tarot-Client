import { combineReducers } from 'redux';
// import someReducer from './someReducer';

const rootReducer = combineReducers({
  // someReducer, // Add more reducers as needed
});

export default rootReducer;
